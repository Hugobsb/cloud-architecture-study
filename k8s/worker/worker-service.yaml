apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: cloud-study
  labels:
    app: worker
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: http
      port: 3001
      targetPort: http
