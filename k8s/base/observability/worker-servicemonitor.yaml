apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: worker
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: worker
  namespaceSelector:
    matchNames:
      - cloud-study
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
