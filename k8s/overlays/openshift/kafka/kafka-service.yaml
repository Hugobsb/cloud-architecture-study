apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: ${OPENSHIFT_PROJECT}
  labels:
    app: kafka
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: kafka
